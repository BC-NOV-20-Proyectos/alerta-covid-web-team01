<%= form_with(model: covid_test) do |form| %>
  <% if covid_test.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(covid_test.errors.count, "error") %> prohibited this covid_test from being saved:</h2>

      <ul>
        <% covid_test.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
