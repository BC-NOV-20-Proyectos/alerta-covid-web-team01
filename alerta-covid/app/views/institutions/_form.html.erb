<%= form_with(model: institution) do |form| %>
  <% if institution.errors.any? %>
    <div id="error_explanation" class="errors">
      <h2><%= pluralize(institution.errors.count, "error") %> prohibited this institution from being saved:</h2>

      <ul>
        <% institution.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="card p-5">
    <div class="form-group row">
      <%= form.label :name, class: "form-control-label col-2" %>
      <div class="col-10">
        <%= form.text_field :name, class: institution.errors.include?(:name) ? "form-control is-invalid" : "form-control" %>
      </div>
    </div>

    <div class="d-flex flex-column ">
      <%= form.submit class: "btn btn-info" %>
    </div>
  </div>
<% end %>
